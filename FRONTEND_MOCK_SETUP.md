# Frontend Mock Data Setup (Member 4)

To run the offline mock server for React frontend development:

1. Ensure Node.js is installed.
2. Run: `npx json-server --watch mock-db.json --port 5000`
3. Access endpoints at `http://localhost:5000/`
