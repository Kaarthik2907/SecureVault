import {NavLink} from "react-router-dom"; import {useAuth} from "../context/AuthContext";
export default function Layout({children}){const {user,logout}=useAuth();const links=[["/","Dashboard"],["/branches","Branches"],["/employees","Employees"],["/vaults","Vaults"],["/requests","Access Requests"],["/monitoring","Security Monitoring"],["/audit","Chain Verification"]];return <div className="app"><aside><h2>🔐 SecureVault</h2><small>{user?.username} · {user?.role}</small><nav>{links.map(([p,n])=><NavLink key={p} to={p}>{n}</NavLink>)}</nav><button onClick={logout}>Logout</button></aside><main>{children}</main></div>}