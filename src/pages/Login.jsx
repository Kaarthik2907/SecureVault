import {useState} from "react"; import {useNavigate} from "react-router-dom"; import {useAuth} from "../context/AuthContext";
export default function Login(){const [username,setU]=useState(""),[password,setP]=useState(""),[error,setE]=useState(""),[busy,setB]=useState(false);const {login}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();setB(true);setE("");try{await login(username,password);nav("/")}catch(e){setE(e.response?.data?.message||"Invalid credentials or backend unavailable")}finally{setB(false)}};return <div className="login"><form onSubmit={submit}><h1>🔐 SecureVault</h1><p>Bank Vault Access Audit System</p><input placeholder="Username" value={username} onChange={e=>setU(e.target.value)}/><input type="password" placeholder="Password" value={password} onChange={e=>setP(e.target.value)}/>{error&&<div className="error">{error}</div>}<button disabled={busy}>{busy?"Signing in...":"Sign in"}</button></form></div>}